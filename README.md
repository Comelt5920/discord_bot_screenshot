# DiscordCap (ดิสคอร์ดแคป)

โปรแกรมบอท Python สำหรับถ่ายหน้าจอ (Screenshot) และส่งรูปไปยังช่อง Discord ที่กำหนดไว้อัตโนมัติ

## ✨ ฟีเจอร์หลัก
- **Manual Capture**: ถ่ายหน้าจอทันทีผ่านคำสั่งใน Discord
- **Auto Report**: ถ่ายหน้าจอและส่งรูปให้อัตโนมัติตามช่วงเวลาที่ตั้งไว้ (เช่น ทุก 30 นาที)
- **Easy GUI**: มีหน้าจอตั้งค่า (Settings) ให้กรอก Bot Token และ Channel ID ได้ง่ายๆ โดยไม่ต้องแก้โค้ด

## 🛠 วิธีการติดตั้ง (คอมพิวเตอร์ที่รัน Python)
1. ดาวน์โหลดโค้ดจาก Repository นี้
2. ติดตั้ง Library ที่จำเป็น:
   ```bash
   pip install discord.py pyautogui
   ```
3. รันโปรแกรมด้วยคำสั่ง:
   ```bash
   python Discord_Cap.py
   ```
4. กรอกข้อมูล **Bot Token** และ **Channel ID** ในหน้าต่างโปรแกรม
5. กดปุ่ม **Start Bot**

## ⌨️ คำสั่งที่ใช้ใน Discord
- `!c` (ค่าเริ่มต้น): สั่งให้บอทถ่ายหน้าจอแล้วส่งมาในแชททันที

## ⚠️ คำเตือน
โปรแกรมนี้ใช้สำหรับส่วนบุคคล โปรดตรวจสอบให้แน่ใจว่าการใช้งานเป็นไปตามนโยบายของ Discord และเคารพความเป็นส่วนตัวของผู้อื่นขณะใช้งานระบบถ่ายหน้าจอ
